# 2026-04-07 Layer0 csynth 关键证据

## 来源

- `summary`: `g:/3DSLED/icocnn/hls_src/HLS/layer0/hls_reports/latest_summary.md`
- `snapshot`: `g:/3DSLED/icocnn/hls_src/HLS/layer0/hls_reports/layer0_hls_prj_sol1_20260407_175035/summary.md`
- `csynth_rpt`: `g:/3DSLED/icocnn/hls_src/HLS/layer0/hls_reports/layer0_hls_prj_sol1_20260407_175035/conv_ico_layer0_csynth.rpt`
- `log`: `g:/3DSLED/icocnn/hls_src/HLS/layer0/vitis_hls.log`

## 关键指标

| 指标 | 数值 |
|---|---:|
| Estimated Clock | `3.981 ns` |
| Total Latency | `4,855,034 cycles` |
| BRAM_18K | `308 / 890` |
| DSP | `147 / 840` |
| FF | `32180 / 407600` |
| LUT | `34386 / 203800` |

## 关键现象

### 1. 主结构目标基本达成

本轮最重要的正面信号：

1. `Loop Constraint Status: All loop constraints were satisfied`
2. 主卷积相关与本地输出后处理相关大环已经普遍达到 `Final II = 1`
3. 旧版最重的“顶层 output 后处理端口冲突”不再以 `II=36` 的形式出现

对应日志证据：

- `vitis_hls.log:324` `Final II = 1`，主卷积大环
- `vitis_hls.log:352` `Final II = 1`，本地输出后处理主环
- `vitis_hls.log:738` `All loop constraints were satisfied`

### 2. PadIco 输入访问仍未完全消除

虽然整体结构已经打通，但 `PadIco` 仍保留输入侧端口冲突：

- `vitis_hls.log:223-231`
- 关键关键词：
  `input_r_load_* due to limited memory ports`
- 对应模块：
  `pad_ico_Pipeline_VITIS_LOOP_84_4`
- 当前结果：
  `Final II = 5`

这说明：

1. `layer0` 的 fast path 已经显著好于旧版大输出冲突热点
2. 但输入重排阶段仍然存在单口读压力

### 3. PadIco 极点求和依赖仍保留

日志中仍可见：

- `vitis_hls.log:245-257`
- 关键关键词：
  `smooth_south_pole_sum`
  `Unable to enforce a carried dependence constraint`
- 对应模块：
  `pad_ico_Pipeline_VITIS_LOOP_115_1`
- 当前结果：
  `Final II = 9`

这说明：

1. 输出后处理热点已被重构掉
2. 当前 `layer0` 剩余更像是前端几何预处理自身的求和依赖，而不是主卷积或顶层输出写回问题

## 结构判断

这轮 `layer0` 的结论很明确：

1. 方向有效，吞吐侧比旧版更接近“热点转移成功”
2. 代价很高，资源从历史低资源版本显著上升到 `BRAM=308`、`DSP=147`
3. 因此这版更像“吞吐优先验证版”，还不是资源友好的最终保留版

## 下一步建议

1. 优先检查 `local_output/local_post` 是否存在可压缩的存储展开与自动 partition 过度问题。
2. 针对 `PadIco` 的 `input_r_load_* due to limited memory ports`，优先尝试更轻量的局部输入 staging，而不是继续扩大输出侧并行度。
3. 针对 `smooth_south_pole_sum` 的 `Final II = 9`，考虑将极点求和进一步改成更显式的小规模并行归约。
