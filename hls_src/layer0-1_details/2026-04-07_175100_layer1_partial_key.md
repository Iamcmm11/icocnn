# 2026-04-07 Layer1 partial csynth 关键证据

## 来源

- `log`: `g:/3DSLED/icocnn/hls_src/HLS/layer1/vitis_hls.log`
- `project_dir`: `g:/3DSLED/icocnn/hls_src/HLS/layer1/layer1_hls_prj`
- 说明：
  本轮 `layer1` 综合运行超过 30 分钟后仍未产出完整 `csynth` 报告，因此本条记录是“中途阻碍证据快照”，不是完整 summary。

## 当前已暴露的关键信息

### 1. 主 partial-sum 路径已被压到 II=1

日志证据：

- `vitis_hls.log:360`
- 模块：
  `conv_ico_layer1_Pipeline_VITIS_LOOP_429_11`
- 结果：
  `Target II = 1, Final II = 1, Depth = 78`

同时日志给出的关键路径为：

- `vitis_hls.log:363-366`
- 关键关键词：
  `ri_partial`
  `fadd`
  `store ... on array 'ri_partial_743'`

这说明：

1. 本轮重构已经把原始 `sum += ...` 主瓶颈切换到了局部 `ri_partial` 路径
2. 至少从调度角度看，`ri_partial` 主环已经成功实现 `II = 1`

### 2. 新的主要阻碍转移到 output_tile 归并环

日志证据：

- `vitis_hls.log:381-390`
- 模块：
  `conv_ico_layer1_Pipeline_VITIS_LOOP_453_13`
- 结果：
  `Target II = 1, Final II = 4, Depth = 32`

关键告警：

1. `Unable to enforce a carried dependence constraint`
2. `store ... output_tile_addr_write_ln467`
3. `load ... output_tile_load`
4. `distance = 8`

这说明当前新的主障碍已经非常清晰：

`ri_partial -> output_tile` 的归并阶段，仍然存在对 `output_tile` 的读改写依赖链。

### 3. 归并环也是当前最强时序热点

同一模块还给出：

- `vitis_hls.log:388`
- `Estimated clock period = 6.774 ns`

关键路径组成：

- `output_tile_load`
- 多级 `sparsemux`
- `fadd reduced`

这说明：

1. `output_tile` 归并不仅把 `II` 拉到 `4`
2. 还形成了当前最坏时序路径
3. 也因此极可能是导致整轮综合异常漫长的主要热点之一

### 4. PadIco 依赖问题仍在，但已不是唯一主线

日志中仍可看到：

- `vitis_hls.log:217-229`
- 模块：
  `pad_ico_Pipeline_VITIS_LOOP_98_4_VITIS_LOOP_99_5`
- 结果：
  `Final II = 9`
- 关键词：
  `smooth_south_pole_sum`
  `carried dependence`

但与当前 `output_tile` 归并环相比，这部分更像“旧问题仍在”，而不是这轮最核心的新热点。

## 阶段结论

这轮 `layer1` 即使没有完整 `csynth` summary，也已经提供了很重要的结构证据：

1. 旧版“全局 output 后处理冲突”为主的瓶颈，已经被成功迁移。
2. `ri_partial` 路径本身已经能做到 `Final II = 1`。
3. 新版最主要的阻碍集中在 `output_tile` 归并环：
   `Final II = 4` + 最坏时序路径。

因此，这轮应被记录为：

`结构方向成立，但 output_tile 归并策略仍需继续拆分`

## 下一步建议

1. 优先改写 `output_tile` 归并阶段，避免在同一个 pipeline 环内执行“先 load output_tile，再 reduce，再 store output_tile”。
2. 可优先尝试把 `output_tile` 初值读取与 `ri_partial` 归并拆成两级局部寄存/局部 bank 化归约，而不是直接原位回写。
3. 在继续追求完整 `csynth` 前，先把 `VITIS_LOOP_453_13` 这段结构改轻，否则综合时间和时序都不稳定。
