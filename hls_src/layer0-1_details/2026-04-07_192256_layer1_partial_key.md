# 2026-04-07 Layer1 partial csynth 关键证据

## 来源

- `log`: `g:/3DSLED/icocnn/hls_src/HLS/layer1/vitis_hls.log`
- `project_dir`: `g:/3DSLED/icocnn/hls_src/HLS/layer1/layer1_hls_prj`
- 说明：本轮综合运行超过 30 分钟后仍未产出完整 `csynth` summary，因此本条记录是中途阻碍快照，不是完整 summary

## 与上一轮 partial 的关系

上一轮快照：`2026-04-07_175100_layer1_partial_key.md`

本轮最重要的变化不是“问题已经消失”，而是“热点已经迁移”：

1. 上一轮主热点在 `output_tile` 原位归并
2. 本轮改成 `ro_accum` 两级归并后，热点从 `output_tile` 迁移到 `ro_accum`
3. `ri_partial` 主计算环继续保持 `II = 1`
4. 最坏时序从 `6.774 ns` 降到 `5.896 ns`

这说明结构方向是对的，但归并阶段仍然没有彻底切断 carried dependence。

## 当前已暴露的关键信息

### 1. 主 partial-sum 环保持 II=1

日志证据：

- `vitis_hls.log:375-379`
- 模块：`conv_ico_layer1_Pipeline_VITIS_LOOP_447_13`
- 结果：`Target II = 1, Final II = 1, Depth = 78`

这说明主乘加路径并没有退化，当前“跑很久”的核心原因不在 `ri_partial` 主环。

### 2. 新热点已经迁移到 ro_accum 归并环

日志证据：

- `vitis_hls.log:396-406`
- 模块：`conv_ico_layer1_Pipeline_VITIS_LOOP_471_15`
- 结果：`Target II = 1, Final II = 4, Depth = 32`

关键告警：

1. `Unable to enforce a carried dependence constraint`
2. `store ... ro_accum_addr_write_ln485`
3. `load ... ro_accum_load`
4. `distance = 8`

这表明当前结构虽然不再直接对 `output_tile` 做原位归并，但 `ro_accum` 依然在同一 pipeline 内形成了 load/store 依赖链。

### 3. 最坏时序有所改善，但仍由归并环主导

同一模块日志还给出：

- `Estimated clock period = 5.896 ns`

相比上一轮 `output_tile` 归并主导时的 `6.774 ns`，本轮已经改善，但最坏路径仍在“归并阶段”，而不是主卷积阶段。

### 4. PadIco 旧问题仍在，但已不是本轮首要新热点

日志里依然可以看到：

- `pad_ico_Pipeline_VITIS_LOOP_98_4_VITIS_LOOP_99_5`
- `smooth_south_pole_sum`
- `carried dependence`

因此 `PadIco` 仍有历史依赖问题，但本轮决定综合耗时和时序的关键因素，已经更明确地落在 `ro_accum` 归并环。

## 阶段结论

本轮 layer1 可以明确记为：

1. `ri_partial` 主环仍然稳定在 `Final II = 1`
2. 原先 `output_tile` 归并导致的热点已经被迁移
3. 新的主阻碍变成 `ro_accum` 归并环上的 carried dependence
4. 时序热点有所改善，但综合仍慢到无法在 30 分钟内稳定出完整 csynth

因此，本轮应定义为：

`热点迁移成功，但归并环仍未彻底解耦；layer1 跑很久的核心原因已从 output_tile 原位归并转移到 ro_accum 原位归并`

## 下一步建议

1. 下一步继续拆 `ro_accum` 归并，避免在同一 pipeline 中做 `load ro_accum -> add reduced -> store ro_accum`
2. 优先尝试把 `ri` 归约完全落到更细粒度寄存器标量后，再统一写入 `ro_accum` 或 `output_tile`
3. 在未切断 `VITIS_LOOP_471_15` 的依赖前，不建议继续追求完整 csynth，因为综合时间和时序都仍会被这段主导
