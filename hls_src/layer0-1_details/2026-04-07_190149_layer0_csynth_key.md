# 2026-04-07 Layer0 csynth 关键证据

## 来源

- `summary`: `g:/3DSLED/icocnn/hls_src/hls_reports/latest_summary.md`
- `snapshot`: `g:/3DSLED/icocnn/hls_src/hls_reports/layer0_hls_prj_sol1_20260407_190149/summary.md`
- `csynth_rpt`: `g:/3DSLED/icocnn/hls_src/hls_reports/layer0_hls_prj_sol1_20260407_190149/conv_ico_layer0_csynth.rpt`
- `log`: `g:/3DSLED/icocnn/hls_src/HLS/layer0/vitis_hls.log`

## 关键指标

| 指标 | 数值 |
|---|---:|
| Estimated Clock | `3.981 ns` |
| Total Latency | `3,692,262 cycles` |
| BRAM_18K | `212 / 890` |
| DSP | `147 / 840` |
| FF | `31,363 / 407,600` |
| LUT | `33,114 / 203,800` |

## 与上一轮 17:50 版本对比

上一轮快照：`2026-04-07_175035_layer0_csynth_key.md`

1. `Total Latency` 从 `4,855,034` 降到 `3,692,262`
2. `BRAM` 从 `308` 降到 `212`
3. `FF` 从 `32,180` 降到 `31,363`
4. `LUT` 从 `34,386` 降到 `33,114`
5. `DSP` 仍为 `147`，尚未回落

这说明去掉 `local_post` 第二整帧缓冲是有效的，确实回收了一部分额外存储和复制开销，但还没有回到旧版低资源区间。

## 关键现象

### 1. 路径统一已经生效

本轮 summary/csynth 快照已经稳定落到共享目录 `hls_src/hls_reports`，不再写入 `hls_src/HLS/layer0/hls_reports`。

这意味着后续 `layer0/layer1/2-5` 的报告路径已经统一，可以和 `layer2-5` 使用同一套归档方式。

### 2. 输出后处理热点没有回到旧版

日志里已经没有旧版那种顶层 `output` 后处理形成的高 II 热点；并且本轮综合给出了：

- `Loop Constraint Status: All loop constraints were satisfied`

这说明当前 `single local_output + direct writeback` 方案保住了最重要的结构收益：输出端全局读改写冲突没有回潮。

### 3. 剩余主阻碍回到 PadIco

本轮最明确的剩余热点已经集中在 `pad_ico`：

1. `VITIS_LOOP_84_4`
   - 证据：`input_r_load_* due to limited memory ports`
   - 结果：`Final II = 5`
2. `VITIS_LOOP_115_1`
   - 证据：`smooth_south_pole_sum`
   - 结果：`Unable to enforce a carried dependence constraint`
   - 最终：`Final II = 9`

这和上一轮判断一致，说明 layer0 当前已经不是“输出后处理拖慢一切”，而是“前端 PadIco 端口冲突与极点求和依赖”在继续限制吞吐。

## 结构判断

本轮 layer0 的结论可以明确写成：

1. 单缓冲替代双整帧缓冲是正确修正，显著降低了最新结构版的回退幅度
2. 但 `local_output` 仍然让资源明显高于旧版基线，尤其 `DSP` 还没有回落
3. layer0 的主问题已经收敛到 `PadIco`，不是输出后处理

因此，这一版应视为：

`在保持输出侧结构收益的前提下，成功回收了一部分本地缓冲代价，但仍需继续压缩 PadIco 热点与局部存储成本`

## 下一步建议

1. 优先针对 `PadIco` 的 `input_r` 端口冲突做更轻量的输入 staging，而不是再扩大输出侧结构
2. 将 `smooth_south_pole_sum` 改成更显式的小规模并行归约，尝试把 `Final II = 9` 继续压低
3. 若资源仍高，下一步先检查 `local_output` 的自动 partition 是否过度，再决定是否进一步缩小本地缓冲组织
