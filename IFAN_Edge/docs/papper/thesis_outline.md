# IFAN-Edge 硕士论文目录草案

## 说明

- 本目录结合当前已完成的 `icoCNN baseline`、`PHAT + LMS` 双特征、`DFA-IcoNet`、`DFA-IcoNet-Edge`、`pre_readout MABA`、LOCATA 评测、DCASE 迁移测试与 FPGA/HLS 映射思路整理。
- 当前目标不是一次定稿，而是先形成一份可直接扩写的论文骨架，后续再根据实验补图、结果固化和学校格式要求继续细化。
- 章节组织遵循“绪论 -> 理论基础 -> 方法设计 -> 边缘优化与硬件映射 -> 实验分析 -> 总结展望”的主线。

---

## 摘要

## ABSTRACT

---

## 第 1 章 绪论

### 1.1 研究背景与意义

### 1.2 国内外研究现状

#### 1.2.1 基于阵列信号处理的声源定位研究现状

#### 1.2.2 基于深度学习的声源定位研究现状

#### 1.2.3 基于球面网格与二十面体卷积的声源定位研究现状

#### 1.2.4 面向边缘部署的神经网络轻量化与 FPGA 加速研究现状

### 1.3 研究问题与挑战

### 1.4 本文主要研究内容

### 1.5 本文组织结构

写作提示：

- 第 1 章可以先按通用论文结构起稿，再逐步替换成与你的工作强相关的表述。
- `1.2` 的四个小节基本可以覆盖你后面第三、四章的方法来源，不容易写散。
- `1.4` 建议直接写成四点：双特征构建、融合网络设计、轻量化边缘模型、实验与硬件映射分析。

---

## 第 2 章 相关理论基础与关键技术

### 2.1 声源定位问题描述与方向表示

### 2.2 麦克风阵列时延估计与空间响应原理

### 2.3 SRP-PHAT 特征构建方法

### 2.4 LMS 自适应时延估计方法

### 2.5 二十面体网格建模与 IcoConv 卷积机制

### 2.6 声源定位评价指标

### 2.7 本章小结

写作提示：

- 这一章主要服务后文，不追求“大而全”，重点把你真正用到的理论讲清楚。
- `2.3` 到 `2.5` 基本就是第三章方法设计的理论前置。
- `2.6` 可以统一引入 `RMSAE`、`DOA error`、参数量、`MAC` 等指标，便于第五章直接复用。

---

## 第 3 章 基于双特征融合的二十面体声源定位网络设计

### 3.1 整体框架与设计思路

### 3.2 icoCNN 基线模型及其局限性分析

### 3.3 PHAT + LMS 双特征二十面体输入构建

### 3.4 双分支残差增强与共享注意力融合网络设计

### 3.5 融合特征学习与方向读出机制

### 3.6 模型训练目标与实现流程

### 3.7 本章小结

写作提示：

- 这一章是你的算法主体章，建议围绕 `为什么从 icoCNN 出发`、`为什么需要 PHAT + LMS`、`为什么要做双分支共享注意力融合` 来展开。
- `3.2` 可以先把 baseline 的优势和不足讲清楚，这样 `3.3`、`3.4` 就自然成立。
- `3.5` 可以写 `Fusion Feature -> final head -> channel readout -> R-pooling -> CleanVertices -> SoftArgMax` 这条输出链。
- `3.6` 可写训练损失、输入张量组织、实现模块，对应当前工程链路。

---

## 第 4 章 面向边缘部署的轻量化与硬件映射方法

### 4.1 边缘部署需求与 IcoConv 计算瓶颈分析

### 4.2 DFA-IcoNet-Edge 轻量化结构设计

### 4.3 面向规则稠密计算的通道收缩策略

### 4.4 Pre-readout MABA 时序增强方法

### 4.5 结构化剪枝探索与主线方案取舍

### 4.6 面向 FPGA/HLS 的 ConvIco 数据流映射思路

### 4.7 本章小结

写作提示：

- 这一章不要只写“模型变小了”，而要写“为什么这样变小更适合硬件实现”。
- `4.3` 建议突出 `C16 -> C8` 的 dense channel slimming 和 `DSP tile` 对齐思路。
- `4.4` 写 `pre_readout MABA` 时要强调插入位置在 `final_block` 之后、`channel_readout` 之前。
- `4.5` 可以把 `SAF-lite` 系列作为探索过程写进去，但结论上说明为什么没有成为主线。
- `4.6` 建议用“映射思路”“资源预算”“实现边界”措辞，避免写成已经完成整网 FPGA 实现。

---

## 第 5 章 实验设计与结果分析

### 5.1 数据集与实验平台

### 5.2 评价指标与对比模型

### 5.3 双特征输入可视化分析

### 5.4 模拟四场景实验结果分析

### 5.5 LOCATA 真实数据集实验结果分析

### 5.6 轻量化模型资源与精度折中分析

### 5.7 消融实验分析

### 5.8 DCASE 跨数据集迁移与数据口径扩展分析

### 5.9 本章小结

写作提示：

- 第五章是结果章，建议把“主结果、消融、扩展实验”分层写清楚，避免混在一起。
- `5.4` 重点写 four-scene 和 hard-scene，突出复杂声学环境表现。
- `5.5` 重点写 LOCATA `task1/task3/task5`、with silences、without silences。
- `5.6` 用参数量、`MAC`、平均 `RMSAE` 做 Pareto 或表格分析，突出 `DFA-IcoNet-Edge` 的边缘价值。
- `5.8` 作为扩展章节较合适，写 DCASE `strict -> non-strict -> IFAN-adapted` 的数据口径变化和当前观察，不建议喧宾夺主。

---

## 第 6 章 总结与展望

### 6.1 本文工作总结

### 6.2 本文创新点总结

### 6.3 存在的问题与不足

### 6.4 后续研究展望

写作提示：

- `6.2` 可先按三点准备：双特征二十面体输入、融合网络与轻量化设计、面向 FPGA/HLS 的边缘部署分析。
- `6.3` 可以如实写：DCASE 主线尚未完全收束、整网 FPGA 资源闭合未完成、部分结果仍需补充 tracking 图和更大口径验证。
- `6.4` 可以写多源场景、SELD 扩展、完整前端硬件化、更系统的定点量化与上板验证。

---

## 当前推荐写作顺序

1. 先完成第 1 章和第 3 章初稿，尽快把“研究问题”和“方法主线”固定下来。
2. 再写第 4 章，把轻量化和硬件映射口径统一成一条完整叙事。
3. 第 5 章最后集中补图、补表、补结果分析，避免现在反复改数字。
4. 第 2 章可穿插写，但最好服务于第 3 章和第 4 章，不要脱离本文主线单独堆概念。
5. 第 6 章放到最后统一回收创新点、不足和展望。

## 当前每章材料成熟度

| 章节 | 当前成熟度 | 说明 |
| --- | --- | --- |
| 第 1 章 | 中 | 结构成熟，需补文献和规范化表述。 |
| 第 2 章 | 中偏低 | 理论点明确，但还需系统整理参考文献和公式。 |
| 第 3 章 | 高 | 已有较完整的方法主线和工程实现支撑。 |
| 第 4 章 | 高 | 轻量化、MABA、硬件映射思路已经比较清晰。 |
| 第 5 章 | 中偏高 | 主结果较完整，但仍需补 tracking 图、最终表格和部分解释。 |
| 第 6 章 | 中 | 可在前面章节稳定后快速收束。 |

## 下一步建议

- 先基于本目录单独起一个“第 1 章绪论”草稿。
- 再起一个“第 3 章方法设计”草稿，把已有中期 PPT 内容转成论文语言。
- 第 4 章和第 5 章可以边补实验边收束，不急着一次写满。
