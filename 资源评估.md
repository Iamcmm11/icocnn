# IcoTempCNN 主干网络资源评估（Kintex-7 xc7k325t）

## 1. 评估范围与假设

本评估仅针对你当前主干网络（不含音频前端 STFT/GCC-PHAT/SRP）：

- 模型：`IcoTempCNN(r=2, C=32, Cin=1, smooth_vertices=True)`
- 输入主干张量：`[B, C, T, charts, H, W] = [1, 1, 103, 5, 4, 8]`
- 主干深度：7 个 `ConvIco + CausConv1d` block（Layer0~Layer6）
- 其中 Layer1 后做一次 `PoolIco`，空间从 `5x4x8` 降到 `5x2x4`
- 估算口径：MAC、参数量、片上存储压力、DSP/BRAM可行性（工程估算，非综合报告）

板卡资源（xc7k325t）按项目内现有口径：

- LUT: 203,800
- FF: 407,600
- DSP48E1: 840
- BRAM36: 445（约 2.0 MB 片上RAM）

---

## 2. 主干计算量（MAC）估算

### 2.1 ConvIco 部分

`ConvIco` 在实现上等价为 3x3 卷积（理论上有效邻域是 7，工程实现常按 9 点卷积计算）。

- Layer0：`~0.028B MAC`
- Layer1：`~5.468B MAC`
- Layer2~Layer6（5层）：每层 `~1.367B MAC`，合计 `~6.835B MAC`

ConvIco 合计：

- 按 3x3=9 点计：`~12.331B MAC`
- 按有效 7 邻域计：`~9.591B MAC`

### 2.2 Temporal Conv1d 部分

- Layer0,1 的时间卷积（960 序列）：每层 `~0.506B MAC`
- Layer2~5（240 序列）：每层 `~0.127B MAC`，合计 `~0.506B MAC`
- Layer6（32->1）：`~0.004B MAC`

Temporal 合计：`~1.522B MAC`

### 2.3 主干总MAC

- 保守（按9点卷积）：`~13.853B MAC / 次推理（T=103）`
- 乐观（按7邻域有效计算）：`~11.113B MAC / 次推理（T=103）`

结论：计算瓶颈主要在 `Layer1` 及后续 `Cin=32, Rin=6 -> Cout=32, Rout=6` 的 ConvIco。

---

## 3. 参数量与存储压力

## 3.1 参数量

主干参数总量约：`290,017`

- FP32：约 `1.11 MB`
- FP16：约 `0.55 MB`
- INT8：约 `0.28 MB`

参数量本身不大，核心问题是中间激活和索引/重排表的存储方式。

### 3.2 激活存储（若整段T一次性缓存）

仅一个大特征图（`[1,103,32,6,5,4,8]`）就有 `3,164,160` 元素：

- FP32：约 `12.66 MB`
- FP16：约 `6.33 MB`
- INT8：约 `3.16 MB`

这已经远超片上 BRAM（约2.0MB）。  
因此**不能用“全帧全层片上缓存”方案**，必须做流式/分块。

### 3.3 索引表风险（重点）

如果直接保存完整 `kernel_expansion_idx`（尤其 Layer1~6 的大张量），片上存储会非常吃紧，甚至不可接受。  
工程上应改为：

- 固定模式索引“在线生成”或模板化复用
- 避免每层完整大表常驻BRAM

---

## 4. 与 xc7k325t 的可行性对比

## 4.1 不同实现策略结论

1. 纯 FP32 + 高并行 + 全量片上缓存：不可行  
原因：DSP/BRAM 都会顶满，且激活缓存明显超片上容量。

2. 纯 FP32 + 流式/分块 + 计算单元复用：可做但很紧  
原因：BRAM 会非常紧张，DSP 和时序压力都偏高，调优成本大。

3. FP16 或 INT8 + 流式/分块 + 复用：可行且工程风险明显下降  
其中 INT8 在 DSP、BRAM、带宽、功耗方面最有优势。

## 4.2 吞吐直觉（用于规划并行度）

按总计算量 `11.1B~13.9B MAC`、时钟 200MHz 粗估：

- 64 MAC并行：约 `0.86s~1.08s` / 次（T=103）
- 128 MAC并行：约 `0.43s~0.54s` / 次
- 256 MAC并行：约 `0.22s~0.27s` / 次

说明：此网络可通过“中等并行 + 流式”落地，但若你追求更低延迟/更低资源，量化几乎是必选项。

---

## 5. 最终判断

针对 `Kintex-7 xc7k325t`：

- **不做量化也不是绝对不能做**，但会进入“资源紧+实现复杂+时序风险高”的状态。
- **若目标是以最小资源稳定部署主干网络，建议先量化再做加速**。

一句话结论：

- **“可实现”前提：流式架构 + 单元复用 + 索引压缩**
- **“更优实现”前提：INT8（或至少FP16）量化 + 适度通道裁剪**

---

## 6. 建议的减肥路线（按优先级）

1. 先做量化：`FP32 -> INT8`（优先做 PTQ，再考虑 QAT）  
2. 索引压缩：`kernel_expansion_idx` 不落大表，改在线生成/模板共享  
3. 流式化：按 `T` 维和通道分块，避免全序列激活常驻  
4. 结构裁剪：`C=32 -> 24/16`（先从 Layer2~Layer6 开始）  
5. 后处理外移：`SoftArgMax` 可保留在 CPU 侧，减少 FPGA 复杂非线性代价

---

## 7. 本文用途

本文是架构级资源预估，用于确定方向。  
下一步建议基于你现有 Layer0 HLS 代码做两组真实综合基线：

- Baseline-A：FP32，低并行复用
- Baseline-B：INT8（或FP16），同等吞吐目标

对比两组 `DSP/BRAM/LUT/时序` 报告后，再最终确定全主干部署策略。

