# IcoTempCNN 主干网络资源评估（Kintex-7 xc7k325t）

## 1. 评估范围与假设

本评估仅针对你当前主干网络（不含音频前端 STFT/GCC-PHAT/SRP）：

- 模型：`IcoTempCNN(r=2, C=32, Cin=1, smooth_vertices=True)`
- 输入主干张量：`[B, C, T, charts, H, W] = [1, 1, 103, 5, 4, 8]`
- 主干深度：7 个 `ConvIco + CausConv1d` block（Layer0~Layer6）
- 其中 Layer1 后做一次 `PoolIco`，空间从 `5x4x8` 降到 `5x2x4`
- 估算口径：MAC、参数量、片上存储压力、DSP/BRAM可行性，并结合现有 `layer1`、`layer2_5` HLS 报告做回看修正

板卡资源（xc7k325t）按项目内现有口径：

- LUT: 203,800
- FF: 407,600
- DSP48E1: 840
- BRAM36: 445（约 2.0 MB 片上RAM）

补充说明：

- HLS 报告通常按 `BRAM_18K` 统计，上述 `BRAM36 = 445` 等价于 `BRAM_18K = 890`
- 本文中的 MAC 估算仍然有价值，但它更适合判断“计算规模”，不应直接当作端到端 latency 预测

### 1.1 结合最新实验后的快速校验

以下数据用于校验本文原有结论是否合理：

| 对象 | 报告/来源 | 主要实现特征 | BRAM_18K | DSP | LUT | Latency (cycles) | 当前启示 |
|---|---|---|---:|---:|---:|---:|---|
| Layer0 | `summary_history.csv` 中 `2026-03-08T11:35:19` 快照 | 早期 FP32 HLS 版本 | 371 | 179 | 41360 | 8926322 | 早期实现已经证明前层很容易吃掉大量 BRAM/DSP |
| Layer1 | `hls_src/hls_reports/layer1_latest_summary.md` | 当前版本仍以 `float` 为主，架构复用不足 | 138 | 11 | 11512 | 24993887933 | 延迟瓶颈并不一定来自 DSP 数量，更可能来自访存与流水组织 |
| Layer2-5 | `hls_src/hls_reports/layer2_5_latest_summary.md` | 顶层接口保留 `float`，内部已引入 `ap_fixed` 混合精度与分块流水 | 64 | 72 | 69382 | 261686621 | 量化/分块有效，但优化后 LUT 已开始成为更突出的成本 |

对照看，原文的大方向总体是对的，但“主要风险在 DSP/BRAM”这句话现在需要改得更细一些。

---

## 2. 主干计算量（MAC）估算

### 2.1 ConvIco 部分

`ConvIco` 在实现上等价为 3x3 卷积（理论上有效邻域是 7，工程实现常按 9 点卷积计算）。

- Layer0：`~0.028B MAC`
- Layer1：`~5.468B MAC`
- Layer2~Layer6（5层）：每层 `~1.367B MAC`，合计 `~6.835B MAC`

ConvIco 合计：

- 按 3x3=9 点计：`~12.331B MAC`
- 按有效 7 邻域计：`~9.591B MAC`

### 2.2 Temporal Conv1d 部分

- Layer0,1 的时间卷积（960 序列）：每层 `~0.506B MAC`
- Layer2~5（240 序列）：每层 `~0.127B MAC`，合计 `~0.506B MAC`
- Layer6（32->1）：`~0.004B MAC`

Temporal 合计：`~1.522B MAC`

### 2.3 主干总MAC

- 保守（按9点卷积）：`~13.853B MAC / 次推理（T=103）`
- 乐观（按7邻域有效计算）：`~11.113B MAC / 次推理（T=103）`

结论：计算瓶颈主要在 `Layer1` 及后续 `Cin=32, Rin=6 -> Cout=32, Rout=6` 的 ConvIco。

补充说明：

- 这个结论适合用来判断“哪一层计算更重”
- 但从现有 HLS 结果看，真实 latency 对 `II`、访存端口、tile 复用、流水拆分更敏感
- 例如 `layer1` 只用了 `11 DSP`，却仍然有 `24993887933 cycles` 的总 latency，说明 MAC 数量并不是端到端性能的唯一主导项

---

## 3. 参数量与存储压力

## 3.1 参数量

主干参数总量约：`290,017`

- FP32：约 `1.11 MB`
- FP16：约 `0.55 MB`
- INT8：约 `0.28 MB`

参数量本身不大，核心问题是中间激活和索引/重排表的存储方式。

### 3.2 激活存储（若整段T一次性缓存）

仅一个大特征图（`[1,103,32,6,5,4,8]`）就有 `3,164,160` 元素：

- FP32：约 `12.66 MB`
- FP16：约 `6.33 MB`
- INT8：约 `3.16 MB`

这已经远超片上 BRAM（约2.0MB）。  
因此**不能用“全帧全层片上缓存”方案**，必须做流式/分块。

### 3.3 索引表风险（重点）

如果直接保存完整 `kernel_expansion_idx`（尤其 Layer1~6 的大张量），片上存储会非常吃紧，甚至不可接受。  
工程上应改为：

- 固定模式索引“在线生成”或模板化复用
- 避免每层完整大表常驻BRAM

---

## 4. 与 xc7k325t 的可行性对比

### 4.1 被实验验证、应当保留的结论

1. 全帧全层片上缓存不可行  
原因：单个大激活图就已明显超出片上 BRAM 容量，这一点没有被后续实验推翻。

2. 流式化、分块化、索引压缩是主线方案  
原因：`layer1` 与 `layer2_5` 的资源和 latency 表现都说明，“先解决数据流和访存结构”比单纯堆算力更关键。

3. 量化或混合精度非常有效  
原因：`layer2_5` 当前版本在接口仍保留 `float` 的情况下，内部已使用 `ap_fixed` 类型，配合 tile/pipeline 后把资源和 latency 都拉回到了更可控区间。

### 4.2 需要修正的地方

1. 原文对 DSP/BRAM 的强调偏重，应该改成“架构与 LUT 风险同样重要”  
当前 `layer2_5` 最新报告中，`DSP = 72/840`、`BRAM_18K = 64/890` 都不高，但 `LUT = 69382/203800` 已达 `34.04%`，说明优化后的主要代价已经部分转移到控制和数据通路逻辑。

2. “纯 FP32 + 流式/分块 + 复用：可做但很紧”这句话要更谨慎  
从 `layer1` 的结果看，当前更大的问题不是“资源快用完”，而是“资源没用满但架构效率很差”。  
也就是说，纯 FP32 方案的风险不仅是资源，还包括极高 latency 和很差的流水利用率。

3. “量化几乎是必选项”要改成更贴近实验的说法  
更准确的表述应是：  
量化/混合精度是目前最划算、最有实证支持的优化方向，但它不是唯一变量；`layer1` 先做结构级重构，收益可能比直接继续压位宽还更大。

### 4.3 对原吞吐估算的修正

按总计算量 `11.1B~13.9B MAC`、时钟 200MHz 推出来的 `0.22s~1.08s`，更适合作为理想化下界，不应直接视作 HLS 最终 latency。

原因有三点：

- 它默认 MAC 能高效连续供数，但真实设计里常被访存端口和 loop-carried dependence 破坏
- 它没有计入 pad/reorder/post-process 等非 MAC 主体逻辑
- 它没有反映顶层是否真正形成 dataflow 或高效 pipeline 重叠

因此，后续做全网络规划时：

- MAC 估算继续保留，用于判断相对计算强度
- 性能预期则应优先参考逐层综合报告，而不是只按 MAC 并行度线性换算

---

## 5. 最终判断

针对 `Kintex-7 xc7k325t`：

- **“全帧缓存 + 直接平推”不可行**，这一点已经被激活规模和现有实验共同验证。
- **当前主矛盾应表述为“架构效率不足”，而不是简单的“DSP/BRAM 不够”**。
- **若目标是以较低风险在 xc7k325t 上落地主干，混合精度 + 流式分块 仍然是最稳妥方向**。

一句话结论：

- **“可实现”前提：流式架构 + 单元复用 + 索引压缩**
- **“性能可接受”前提：先把 `layer1` 这类高 latency 结构改造成更像 `layer2_5` 的分阶段流水**
- **“更优实现”前提：在上述结构基础上继续推进 `INT8/FP16/ap_fixed` 混合精度与适度通道裁剪**

---

## 6. 建议的减肥路线（按优先级）

1. 先重构 `layer1`：优先把它改造成 `stage/pad/conv/post-process/writeback` 这类可分段流水结构  
2. 再做位宽实验：围绕 `ap_fixed` 的 `input/weight/act/acc` 宽度做 sweep，而不是只讨论抽象的 `FP32 -> INT8`  
3. 索引压缩：`kernel_expansion_idx` 不落大表，改在线生成或模板共享  
4. 流式化：按 `T` 维和通道/输出通道 tile 分块，避免全序列激活常驻  
5. 结构裁剪：若全网络累计 LUT/BRAM 预算仍偏高，再考虑 `C=32 -> 24/16`  
6. 后处理外移：`SoftArgMax` 可保留在 CPU 侧，减少 FPGA 上复杂非线性逻辑

---

## 7. 本文用途

本文仍然是架构级资源预估，但现在已经不是“纯理论判断”，而是加入了 `layer1`、`layer2_5` 的真实综合结果做校验。

下一步更合适的基线不再是只做“FP32 vs INT8”两组，而是至少并行推进三件事：

- Baseline-A：把 `layer1` 做成与 `layer2_5` 同类的结构级优化版本
- Baseline-B：在 `layer2_5` 当前结构上做位宽 sweep，对比 `DSP/BRAM/LUT/latency`
- Baseline-C：按逐层峰值资源做整网预算，重点检查累计 LUT、BRAM 和 buffering 方案是否闭合

这样得到的结论会比单纯的理论 MAC 估算更能指导最终部署策略。
